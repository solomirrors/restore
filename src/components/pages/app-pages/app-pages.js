import React, {Component} from "react";
import AppHeader from "./fragments/app-header";

export default class AppPages extends Component{
    render() {
        return(
            <React.Fragment>
                <AppHeader/>
            </React.Fragment>
        )
    }
}